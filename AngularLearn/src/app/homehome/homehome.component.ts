import { Component } from '@angular/core';

@Component({
  selector: 'app-homehome',
  templateUrl: './homehome.component.html',
  // No need for styleUrl as we're using Tailwind CSS classes directly
})
export class HomehomeComponent {
  
}
